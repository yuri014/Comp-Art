import React from 'react';

import { PostProps } from '@interfaces/Post';

const Share: React.FC<PostProps> = ({ post }) => (
  <div>
    <div className="share">TEMP</div>
  </div>
);

export default Share;
