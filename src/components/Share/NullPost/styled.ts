import styled from 'styled-components';

const NullPostContainer = styled.div`
  padding: 2rem;
  display: grid;

  p {
    text-align: center;
  }

  svg {
    max-width: 30rem;
    max-height: 20rem;
    margin: 0 auto;
  }
`;

export default NullPostContainer;
